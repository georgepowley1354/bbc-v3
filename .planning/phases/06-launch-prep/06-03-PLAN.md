---
phase: 06-launch-prep
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - bbc-v3/index.html
autonomous: false
requirements:
  - LAUNCH-01
  - LAUNCH-02
must_haves:
  truths:
    - "GA4 measurement ID in index.html is a real ID (not G-XXXXXXXXXX)"
    - "All three social links point to real URLs (not href='#')"
  artifacts:
    - path: "bbc-v3/index.html"
      provides: "Real GA4 ID in gtag script and real social link URLs"
      contains: "gtag('config'"
  key_links:
    - from: "bbc-v3/index.html"
      to: "Google Analytics 4"
      via: "gtag.js script src and config call"
      pattern: "gtag\\('config', 'G-[A-Z0-9]+"
    - from: "bbc-v3/index.html"
      to: "Social media profiles"
      via: "social-link anchor hrefs"
      pattern: "class=\"social-link\".*href=\"https://"
---

<objective>
Replace the GA4 placeholder ID and social link placeholders with real values provided by the site owner.

Purpose: These are the final two placeholder items blocking launch. The site cannot ship with a fake analytics ID or dead social links.
Output: index.html with real GA4 measurement ID and real social media URLs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@bbc-v3/index.html
</context>

<interfaces>
<!-- Current placeholder locations in bbc-v3/index.html -->
Line 58: <!-- Google Analytics 4 — replace G-XXXXXXXXXX with your measurement ID -->
Line 59: <script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
Line 64: gtag('config', 'G-XXXXXXXXXX');

Line 1180: <a href="#" class="social-link" aria-label="Big Bad Coding on Instagram" ...>
Line 1187: <a href="#" class="social-link" aria-label="Big Bad Coding on Facebook" ...>
Line 1192: <a href="#" class="social-link" aria-label="Big Bad Coding on LinkedIn" ...>
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Owner provides GA4 measurement ID</name>
  <read_first>bbc-v3/index.html</read_first>
  <action>
The site owner must provide their Google Analytics 4 measurement ID. This is found in:
- Google Analytics (analytics.google.com) -> Admin -> Data Streams -> select the web stream -> Measurement ID

The ID format is `G-` followed by 10 alphanumeric characters (e.g., `G-ABC1234DEF`).

Once provided, the executor will replace all 3 occurrences of `G-XXXXXXXXXX` in `bbc-v3/index.html`:
1. Line 58: the HTML comment (update to show the real ID)
2. Line 59: the gtag.js script `src` URL parameter `?id=G-XXXXXXXXXX`
3. Line 64: the `gtag('config', 'G-XXXXXXXXXX')` call

Also update the comment on line 58 from "replace G-XXXXXXXXXX with your measurement ID" to "Google Analytics 4" (remove the placeholder instruction).
  </action>
  <resume-signal>Provide the GA4 measurement ID (format: G-XXXXXXXXXX)</resume-signal>
  <verify>
    <automated>cd C:/Users/georg/my-project && grep -c "G-XXXXXXXXXX" bbc-v3/index.html</automated>
  </verify>
  <acceptance_criteria>
- `grep "G-XXXXXXXXXX" bbc-v3/index.html` returns 0 matches (all placeholders replaced)
- `grep -c "gtag('config', 'G-" bbc-v3/index.html` returns exactly 1 match
- `grep "googletagmanager.com/gtag/js?id=G-" bbc-v3/index.html` returns exactly 1 match
- The GA4 ID in the script src matches the GA4 ID in the config call (same value in both places)
- `grep "replace G-XXXXXXXXXX" bbc-v3/index.html` returns 0 matches (instruction comment removed)
  </acceptance_criteria>
  <done>GA4 measurement ID is real (not placeholder) in both the script src and config call</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Owner provides social media URLs</name>
  <read_first>bbc-v3/index.html</read_first>
  <action>
The site owner must provide real URLs for three social media profiles:
1. Instagram (e.g., https://www.instagram.com/bigbadcoding/)
2. Facebook (e.g., https://www.facebook.com/bigbadcoding/)
3. LinkedIn (e.g., https://www.linkedin.com/company/bigbadcoding/)

Once provided, the executor will replace the `href="#"` on each social link in `bbc-v3/index.html`:

**Line 1180 (Instagram):**
Change `href="#"` to `href="https://www.instagram.com/{provided_handle}/"` and add `target="_blank" rel="noopener noreferrer"`

**Line 1187 (Facebook):**
Change `href="#"` to `href="https://www.facebook.com/{provided_page}/"` and add `target="_blank" rel="noopener noreferrer"`

**Line 1192 (LinkedIn):**
Change `href="#"` to `href="https://www.linkedin.com/{provided_path}/"` and add `target="_blank" rel="noopener noreferrer"`

Each link already has `class="social-link"` and an `aria-label` identifying the platform. The `aria-label` text should remain unchanged. The `target="_blank"` and `rel="noopener noreferrer"` attributes are required per SEO-07 (all external links must have rel="noopener noreferrer").
  </action>
  <resume-signal>Provide the three social media URLs: Instagram, Facebook, LinkedIn</resume-signal>
  <verify>
    <automated>cd C:/Users/georg/my-project && grep "social-link" bbc-v3/index.html | grep -c 'href="#"'</automated>
  </verify>
  <acceptance_criteria>
- `grep "social-link" bbc-v3/index.html | grep -c 'href="#"'` returns 0 (no placeholder hrefs remain)
- `grep "social-link" bbc-v3/index.html | grep -c "https://"` returns 3 (all three point to real URLs)
- `grep "social-link" bbc-v3/index.html | grep -c 'rel="noopener noreferrer"'` returns 3
- `grep "social-link" bbc-v3/index.html | grep -c 'target="_blank"'` returns 3
- Each social link href contains a real domain (instagram.com, facebook.com, linkedin.com)
  </acceptance_criteria>
  <done>All three social links (Instagram, Facebook, LinkedIn) point to real URLs with proper security attributes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site -> Google Analytics | GA4 sends page view data to Google; no PII collected by default |
| site -> social platforms | Links open in new tab; no data sent until user clicks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | GA4 | accept | GA4 collects standard analytics (page views, sessions); no custom PII dimensions configured |
| T-06-07 | Spoofing | social links | mitigate | rel="noopener noreferrer" prevents reverse tabnapping; target="_blank" isolates browsing context |
</threat_model>

<verification>
1. `grep "G-XXXXXXXXXX" bbc-v3/index.html` returns 0 matches
2. `grep 'href="#"' bbc-v3/index.html | grep social-link` returns 0 matches
3. All social links have `target="_blank" rel="noopener noreferrer"`
</verification>

<success_criteria>
- GA4 fires with a real measurement ID (verifiable in GA4 DebugView after deploy)
- Social links navigate to real Instagram, Facebook, and LinkedIn profiles
- No placeholder values remain in index.html
</success_criteria>

<output>
After completion, create `.planning/phases/06-launch-prep/06-03-SUMMARY.md`
</output>
