---
phase: "03"
plan: "03-01"
type: build
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tailwind.config.ts
  - postcss.config.js
  - next.config.ts
  - tsconfig.json
  - .eslintrc.json
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/template.tsx
  - src/app/page.tsx
  - src/constants/animation.ts
  - src/types/index.ts
  - src/components/layout/Navigation.tsx
  - src/components/layout/Footer.tsx
  - src/components/ui/Button.tsx
  - src/components/ui/SectionHeader.tsx
  - src/components/ui/MotionDiv.tsx
  - netlify.toml
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run dev starts without errors at localhost:3000"
    - "Navigation renders VERDANT wordmark and nav links"
    - "Nav transitions from transparent to forest-deep background on scroll past 80px"
    - "Mobile hamburger opens full-screen overlay menu"
    - "Footer renders with BBC badge linked to bbc-agency.com"
    - "Cormorant Garamond and DM Sans load (no fallback fonts visible in browser)"
    - "Page transitions animate on route change via template.tsx"
    - "Design system color tokens available as Tailwind utilities (bg-forest-deep, text-sage, etc.)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts, Nav, Footer"
      exports: ["RootLayout"]
    - path: "src/app/template.tsx"
      provides: "Page-enter fade transition"
      exports: ["Template"]
    - path: "src/constants/animation.ts"
      provides: "Shared Framer Motion variants and easing curves"
      exports: ["ease", "fadeUp", "fadeIn", "heroEntrance", "stagger", "slideInLeft"]
    - path: "src/components/layout/Navigation.tsx"
      provides: "Sticky nav with scroll state"
      exports: ["Navigation"]
    - path: "src/components/layout/Footer.tsx"
      provides: "4-col footer with BBC badge"
      exports: ["Footer"]
    - path: "src/components/ui/MotionDiv.tsx"
      provides: "Client-side motion wrappers for use in Server Component trees"
      exports: ["MotionDiv", "MotionSection", "MotionH1", "MotionH2", "MotionP"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "Cormorant_Garamond + DM_Sans with CSS variables"
      pattern: "variable: '--font-display'"
    - from: "tailwind.config.ts"
      to: "CSS variables"
      via: "fontFamily uses var(--font-display) and var(--font-sans)"
      pattern: "var\\(--font-display\\)"
    - from: "src/components/layout/Navigation.tsx"
      to: "scroll state"
      via: "useEffect window.addEventListener('scroll')"
      pattern: "scrollY.*80"
---

<objective>
Scaffold the Next.js 14 application inside the existing verdant/ directory, wire the design system into Tailwind, configure both Google Fonts, and build the global layout components (Navigation, Footer, template.tsx page transitions). This plan establishes the foundation all subsequent plans depend on.

Purpose: Every other plan in Phase 3 depends on the app existing and the design system being wired. Without this, no page can be built.
Output: A running Next.js 14 app at localhost:3000 with design tokens, fonts, navigation, footer, and page transitions all functional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@design-system/MASTER.md
@.planning/phases/03-home-and-portfolio-pages/03-RESEARCH.md

<interfaces>
<!-- Key patterns from RESEARCH.md for the executor. No codebase exploration needed. -->

Font import pattern (next/font/google):
```typescript
import { Cormorant_Garamond, DM_Sans } from 'next/font/google'

const cormorant = Cormorant_Garamond({
  subsets: ['latin'],
  weight: ['300', '400', '600', '700'],   // NOT 'variable' — not a variable font
  style: ['normal', 'italic'],
  variable: '--font-display',
  display: 'swap',
})

const dmSans = DM_Sans({
  subsets: ['latin'],
  weight: ['300', '400', '500'],
  variable: '--font-sans',
  display: 'swap',
})
// Apply to <html>: className={`${cormorant.variable} ${dmSans.variable}`}
```

template.tsx pattern (enter-only — exit animations broken in App Router):
```typescript
"use client"
import { motion } from 'framer-motion'
export default function Template({ children }: { children: React.ReactNode }) {
  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1] }}
    >
      {children}
    </motion.div>
  )
}
```

Tailwind content paths (must use ./src/** because of --src-dir):
```typescript
content: [
  './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
  './src/components/**/*.{js,ts,jsx,tsx,mdx}',
  './src/app/**/*.{js,ts,jsx,tsx,mdx}',
],
```

Animation constants (transcribed verbatim from design-system/MASTER.md):
```typescript
export const ease = {
  out: [0.16, 1, 0.3, 1] as const,
  inOut: [0.76, 0, 0.24, 1] as const,
  spring: { type: 'spring' as const, stiffness: 200, damping: 28 },
}
export const fadeUp = {
  hidden: { opacity: 0, y: 32 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.8, ease: ease.out } },
}
export const fadeIn = {
  hidden: { opacity: 0 },
  visible: { opacity: 1, transition: { duration: 0.6, ease: ease.out } },
}
export const heroEntrance = {
  hidden: { opacity: 0, scale: 1.03 },
  visible: { opacity: 1, scale: 1, transition: { duration: 1.4, ease: ease.out } },
}
export const stagger = {
  visible: { transition: { staggerChildren: 0.12 } },
}
export const slideInLeft = {
  hidden: { opacity: 0, x: -48 },
  visible: { opacity: 1, x: 0, transition: { duration: 0.8, ease: ease.out } },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 14, install dependencies, configure Tailwind and globals</name>
  <files>
    package.json, package-lock.json, tailwind.config.ts, postcss.config.js,
    next.config.ts, tsconfig.json, .eslintrc.json, src/app/globals.css,
    src/constants/animation.ts, src/types/index.ts, netlify.toml
  </files>
  <action>
    Run the scaffold command INSIDE the verdant/ directory (it already exists — the dot means "scaffold here"):

    ```bash
    cd C:/Users/georg/my-project/verdant
    npx create-next-app@14 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack
    ```

    When prompted about existing directory, answer yes to proceed. After scaffold completes:

    ```bash
    npm install framer-motion@11 lucide-react
    ```

    Then replace tailwind.config.ts with the full design system token set:
    - content paths: `./src/pages/**/*.{js,ts,jsx,tsx,mdx}`, `./src/components/**/*.{js,ts,jsx,tsx,mdx}`, `./src/app/**/*.{js,ts,jsx,tsx,mdx}`
    - colors: full palette from MASTER.md section 2 — forest.deep #1C2B1E, forest.mid #2A3E2C, sage #4A7C59, sage.light #6B9E7A, gold #C9A84C, gold.light #D9B96C, stone.warm #F5F0E8, stone.mid #EBE5D9, stone.dark #D4CCC0, off-white #FAF9F6, text-primary #1A1A1A, text-secondary #4A4A4A, text-muted #7A7A6E
    - fontFamily: `display: ['var(--font-display)', 'Georgia', 'serif']`, `sans: ['var(--font-sans)', 'system-ui', 'sans-serif']`
    - spacing extensions: 18→72px, 22→88px, 26→104px, 30→120px, 34→136px, 38→152px, 42→168px, section→128px, section-sm→80px

    Replace src/app/globals.css:
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    @layer base {
      html { scroll-behavior: smooth; }
      *:focus-visible {
        outline: 2px solid #4A7C59;
        outline-offset: 2px;
      }
    }
    ```

    Create src/constants/animation.ts with all six exports: ease, fadeUp, fadeIn, heroEntrance, stagger, slideInLeft. Use exact values from MASTER.md section 5. Add `as const` assertions on array easing values to satisfy TypeScript.

    Create src/types/index.ts with the Project type:
    ```typescript
    export interface Project {
      id: string
      slug: string
      name: string
      category: 'Hardscape' | 'Softscape' | 'Pool' | 'Kitchen' | 'Full Property'
      location: string
      investmentRange: string
      heroImage: string
      galleryImages: string[]
      description: string
      highlights: string[]
    }
    ```

    Update next.config.ts to add Unsplash image support (use remotePatterns — images.domains is deprecated in Next.js 14):
    ```typescript
    images: {
      remotePatterns: [
        {
          protocol: 'https',
          hostname: 'images.unsplash.com',
          pathname: '/**',
        },
      ],
    },
    ```

    Create netlify.toml:
    ```toml
    [build]
      command = "npm run build"
      publish = ".next"

    [[plugins]]
      package = "@netlify/plugin-nextjs"
    ```
  </action>
  <verify>
    <automated>cd C:/Users/georg/my-project/verdant && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npm run build` exits 0. node_modules contains framer-motion and lucide-react. tailwind.config.ts has forest.deep, sage, gold, stone.warm tokens. src/constants/animation.ts exports all six variants. src/types/index.ts exports Project interface.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Root layout with fonts, Navigation and Footer components, template.tsx</name>
  <files>
    src/app/layout.tsx, src/app/template.tsx, src/app/page.tsx,
    src/components/layout/Navigation.tsx, src/components/layout/Footer.tsx,
    src/components/ui/Button.tsx, src/components/ui/SectionHeader.tsx,
    src/components/ui/MotionDiv.tsx
  </files>
  <behavior>
    - Navigation renders VERDANT wordmark (DM Sans, tracking-[0.3em], ALL CAPS, white)
    - Navigation has transparent bg when scrollY is 0, bg-forest-deep/95 backdrop-blur-md when scrollY >= 80
    - Navigation height transitions from h-24 to h-16 on scroll
    - Mobile: hamburger icon visible below md breakpoint, click opens full-screen forest-deep overlay with stagger-animated links
    - Footer renders 4 columns: brand/tagline, nav links, services, contact + BBC badge
    - BBC badge "Built by BBC" links to https://bbc-agency.com, DM Sans 12px, text-muted
    - template.tsx wraps children in motion.div initial opacity:0 → animate opacity:1 over 0.5s
    - Button component accepts variant prop: 'primary' | 'secondary' | 'ghost' | 'ghost-dark'
    - SectionHeader accepts eyebrow, headline, body props; renders gold rule above eyebrow
  </behavior>
  <action>
    Create src/app/layout.tsx using the font pattern from RESEARCH.md (Cormorant_Garamond with explicit weight array, DM_Sans, both as CSS variables on html element). Body gets `bg-stone-warm text-text-primary font-sans antialiased`. Import Navigation and Footer.

    Create src/app/template.tsx as a "use client" Framer Motion fade-in wrapper. Duration 0.5s, ease [0.16, 1, 0.3, 1].

    Replace the default src/app/page.tsx with a minimal placeholder Server Component that renders a centered "Verdant — Coming Soon" in font-display, to confirm fonts work.

    Create src/components/ui/MotionDiv.tsx ("use client") exporting: MotionDiv, MotionSection, MotionH1, MotionH2, MotionP as re-exports of the corresponding motion.* elements from framer-motion.

    Create src/components/ui/Button.tsx ("use client" not needed — no hooks) accepting:
    - variant: 'primary' | 'secondary' | 'ghost' | 'ghost-dark' (default: 'primary')
    - size: 'md' | 'lg' (default: 'md')
    - href?: string (renders as <a> if provided, <button> otherwise)
    - children, className, ...rest
    Styles per MASTER.md section 6: rounded-none, DM Sans 500, 13px, tracking-[0.1em], uppercase. Primary: bg-sage text-white hover:bg-sage-light. Secondary: border border-gold text-gold hover:bg-gold hover:text-forest-deep. Ghost: border border-white/50 text-white hover:border-white hover:bg-white/10. Ghost-dark: border border-forest-deep text-forest-deep hover:bg-forest-deep hover:text-white. All transitions: transition-colors duration-300.

    Create src/components/ui/SectionHeader.tsx accepting eyebrow: string, headline: string, body?: string, dark?: boolean (controls text colors for dark sections). Render: gold 1px horizontal rule (w-12 h-px bg-gold mb-3), then eyebrow (DM Sans 11px tracking-[0.2em] uppercase, text-muted on light/gold on dark), then headline (font-display), then optional body (DM Sans 18px text-secondary).

    Create src/components/layout/Navigation.tsx ("use client"):
    - useEffect + useState to track scrolled (boolean, true when scrollY >= 80)
    - useState for mobileOpen (boolean)
    - Outer: fixed top-0 w-full z-50, transition-all duration-300
    - When !scrolled: bg-transparent h-24
    - When scrolled: bg-forest-deep/95 backdrop-blur-md h-16
    - Left: VERDANT wordmark (DM Sans, tracking-[0.3em], uppercase, text-white)
    - Right (md+): nav links array ['Portfolio', 'Services', 'Process', 'About', 'Contact'] as Next.js Link components. DM Sans 14px tracking-[0.1em] text-white/70 hover:text-white. Plus "Start a Project" Button variant="primary" linking to /contact.
    - Mobile: hamburger icon (Lucide Menu icon, 24px, text-white) at right. When open: full-screen overlay div with z-[100] bg-forest-deep flex flex-col items-center justify-center gap-8. Links rendered as Cormorant Garamond italic 36px text-white. Close button (Lucide X icon) at top-right. Use stagger animation from constants/animation.ts.
    - useReducedMotion check — if true, skip opacity/transform animations.

    Create src/components/layout/Footer.tsx (Server Component — no client directive needed):
    - bg-forest-deep text-white py-16 px-6 md:px-12 lg:px-20
    - Grid: 4 columns on lg, 2 on sm, 1 on mobile
    - Col 1: VERDANT wordmark, tagline "We Build Outdoor Worlds", DM Sans 14px text-white/60
    - Col 2: Navigation links (Portfolio, Services, Process, About, Contact)
    - Col 3: Services links (Hardscape, Softscape, Pools, Outdoor Kitchens, Full Property)
    - Col 4: Contact info placeholder + "Start a Project" Button
    - Bottom bar: border-t border-white/10 pt-8 flex justify-between. Left: © 2026 Verdant Landscape Design. Right: "Built by BBC" link to https://bbc-agency.com — DM Sans 12px text-muted.
  </action>
  <verify>
    <automated>cd C:/Users/georg/my-project/verdant && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    `npm run build` exits 0 with no TypeScript errors. Navigation, Footer, Button, SectionHeader, MotionDiv all exist at their expected paths. layout.tsx uses --font-display and --font-sans CSS variables. template.tsx is a "use client" motion wrapper.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External CDN → Build | Google Fonts fetched by next/font at build time — not at runtime, so no runtime MITM risk |
| Browser → Static Assets | Netlify serves pre-built static files over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information | Navigation.tsx mobile menu | accept | No sensitive data in nav; overlay is cosmetic only |
| T-03-01-02 | Denial of Service | next/font/google build step | accept | Build-time fetch; if CDN unreachable, build fails cleanly — not a runtime risk |
| T-03-01-03 | Spoofing | BBC badge href | accept | href is hardcoded to bbc-agency.com; no user-supplied redirect |
</threat_model>

<verification>
After both tasks complete, run the dev server and confirm:

1. `cd C:/Users/georg/my-project/verdant && npm run dev` starts at localhost:3000
2. Visit localhost:3000 — "Verdant — Coming Soon" renders in Cormorant Garamond (serif display font, not system sans-serif)
3. DM Sans loads for body text
4. Navigation is visible at top — VERDANT wordmark present
5. Scroll down 100px — nav background transitions to dark forest-deep
6. Resize to mobile width (375px) — hamburger icon appears, desktop nav links hidden
7. Click hamburger — full-screen overlay opens with centered italic links
8. Footer is visible at bottom with "Built by BBC" text
9. `npm run build` exits 0 — no TypeScript errors
</verification>

<success_criteria>
- `npm run build` exits 0 with no TypeScript or ESLint errors
- `npm run dev` serves at localhost:3000
- Tailwind utilities bg-forest-deep, text-sage, text-gold, bg-stone-warm resolve correctly (visible in browser DevTools computed styles)
- Cormorant Garamond renders on the placeholder home page (confirm in browser — it is visibly serif and editorial)
- Navigation scroll state change visible at 80px scroll depth
- Mobile menu overlay opens and closes
- Footer includes "Built by BBC" linked to https://bbc-agency.com
- src/constants/animation.ts exports: ease, fadeUp, fadeIn, heroEntrance, stagger, slideInLeft
- src/types/index.ts exports: Project interface
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-and-portfolio-pages/03-01-SUMMARY.md` documenting:
- Exact scaffold command used and result
- Any deviations from the plan (e.g., create-next-app prompts answered)
- Confirmed dependency versions installed (next, framer-motion, lucide-react, tailwindcss)
- Files created
- Any issues encountered and how resolved
</output>
