---
phase: "03"
plan: "03-02"
type: build
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/page.tsx
  - src/components/home/HeroSection.tsx
  - src/components/home/LogoBar.tsx
  - src/components/home/ManifestoSection.tsx
  - src/components/home/ServicesPreview.tsx
  - src/components/home/FeaturedProject.tsx
  - src/components/home/ProcessTeaser.tsx
  - src/components/home/SeasonalBanner.tsx
  - src/components/home/Testimonials.tsx
  - src/components/home/InstagramPlaceholder.tsx
  - src/components/home/FinalCTA.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Home page renders all 10 sections in correct order without console errors"
    - "Hero is full-viewport-height with forest-deep gradient overlay over Unsplash image"
    - "Hero headline renders in Cormorant Garamond 300 at 80-96px desktop / 48-60px mobile"
    - "No broken images — all Unsplash URLs are real and resolvable"
    - "All sections use correct background colors per MASTER.md section 7"
    - "Scroll-triggered animations fire for each section (whileInView, once: true)"
    - "Services Preview shows all 5 service categories with icons"
    - "Testimonials section shows 2 testimonial cards with project attribution"
    - "Final CTA section is full-width dark with primary CTA button"
    - "prefers-reduced-motion is respected — animations skipped when user has set it"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home page Server Component composing all 10 sections"
      exports: ["HomePage"]
    - path: "src/components/home/HeroSection.tsx"
      provides: "Full-viewport hero with cinematic entrance animation"
      exports: ["HeroSection"]
    - path: "src/components/home/ServicesPreview.tsx"
      provides: "5 service cards with Lucide icons"
      exports: ["ServicesPreview"]
    - path: "src/components/home/FeaturedProject.tsx"
      provides: "Large image + project details split layout"
      exports: ["FeaturedProject"]
    - path: "src/components/home/Testimonials.tsx"
      provides: "2-column testimonial cards"
      exports: ["Testimonials"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/home/*"
      via: "named imports, no props required (all data is internal)"
      pattern: "import.*from '@/components/home"
    - from: "src/components/home/HeroSection.tsx"
      to: "src/constants/animation.ts"
      via: "heroEntrance, fadeUp, stagger variants"
      pattern: "heroEntrance|fadeUp"
    - from: "src/components/home/ServicesPreview.tsx"
      to: "inline SVG"
      via: "custom 48x48 stroke-based SVG icons per MASTER.md section 8 — no external icon library"
      pattern: "viewBox=\"0 0 48 48\""
---

<objective>
Build all 10 homepage sections as individual React components and assemble them into the home page. Each section matches the design spec from MASTER.md exactly — correct backgrounds, typography, spacing, animation variants, and Unsplash image URLs.

Purpose: The home page is the primary client-facing showcase. It must demonstrate BBC's capability to $50k+ clients at first glance. This is the highest-visibility deliverable in Phase 3.
Output: A fully built home page at localhost:3000 with all 10 sections rendering correctly, fonts loading, animations firing on scroll, and zero broken images.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@design-system/MASTER.md
@.planning/phases/03-home-and-portfolio-pages/03-RESEARCH.md
@.planning/phases/03-home-and-portfolio-pages/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 03-01 that this plan consumes -->

From src/constants/animation.ts:
```typescript
export const ease = { out: [0.16, 1, 0.3, 1], inOut: [...], spring: {...} }
export const fadeUp     // hidden: {opacity:0, y:32} → visible: {opacity:1, y:0}
export const fadeIn     // hidden: {opacity:0} → visible: {opacity:1}
export const heroEntrance  // hidden: {opacity:0, scale:1.03} → visible: {opacity:1, scale:1}
export const stagger    // staggerChildren: 0.12
export const slideInLeft // hidden: {opacity:0, x:-48} → visible: {opacity:1, x:0}
```

From src/components/ui/Button.tsx:
```typescript
// Props: variant ('primary'|'secondary'|'ghost'|'ghost-dark'), href?, children, className
<Button variant="primary" href="/contact">Start a Project</Button>
```

From src/components/ui/SectionHeader.tsx:
```typescript
// Props: eyebrow, headline, body?, dark?
<SectionHeader eyebrow="OUR SERVICES" headline="Built to Last" dark={false} />
```

From src/components/ui/MotionDiv.tsx:
```typescript
export { MotionDiv, MotionSection, MotionH1, MotionH2, MotionP }
// Use these in "use client" components instead of importing motion.* directly
```

From src/types/index.ts:
```typescript
export interface Project {
  id: string; slug: string; name: string;
  category: 'Hardscape'|'Softscape'|'Pool'|'Kitchen'|'Full Property';
  location: string; investmentRange: string; heroImage: string;
  galleryImages: string[]; description: string; highlights: string[];
}
```

Unsplash URL pattern for this project (use these exact format):
- Photo by ID: `https://images.unsplash.com/photo-{ID}?w=1200&q=80&auto=format&fit=crop`
- Always include w, q, auto, fit params to prevent oversized requests
- Suggested search terms: "stone terrace", "landscape garden luxury", "outdoor pool stone", "outdoor kitchen fireplace", "garden path pergola"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build sections 1–5 (Hero, LogoBar, Manifesto, ServicesPreview, FeaturedProject)</name>
  <files>
    src/components/home/HeroSection.tsx,
    src/components/home/LogoBar.tsx,
    src/components/home/ManifestoSection.tsx,
    src/components/home/ServicesPreview.tsx,
    src/components/home/FeaturedProject.tsx
  </files>
  <behavior>
    - HeroSection: full viewport (min-h-screen), Next.js Image fill with priority, forest-deep gradient overlay (70% bottom to transparent), content bottom-aligned with pb-20 md:pb-28. Gold rule above eyebrow "SARATOGA SPRINGS", headline "We Build" (300 weight) + "Outdoor Worlds." (300 italic) in font-display 80-96px desktop (text-[80px] lg:text-[96px]), subline in DM Sans 18px text-white/70, two buttons: "Start a Project" (primary) + "View Our Work" (ghost). heroEntrance variant on image, fadeUp on content with stagger. Scroll indicator: animated chevron at bottom center.
    - LogoBar: narrow strip py-8, bg-stone-mid, "As Seen In" eyebrow, 5 text-based logos in DM Sans uppercase tracking-[0.2em] text-muted opacity-60: "LUXE Interiors", "Saratoga Living", "Capital Region Life", "Albany Business Review", "The Knot"
    - ManifestoSection: bg-off-white py-section. Two-column lg: left col has SectionHeader eyebrow="OUR PHILOSOPHY" headline="Where Architecture Meets Nature" body (DM Sans 18px, text-secondary, 3 sentences about transforming outdoor spaces). Right col: abstract crop Unsplash landscape image in Next.js Image, aspect-square, object-cover. On mobile: single column stacked.
    - ServicesPreview: bg-stone-warm py-section. SectionHeader eyebrow="WHAT WE BUILD" headline="Five Ways We Transform Your Property". Grid 3-col lg / 2-col md / 1-col sm. 5 service cards, each: stone-mid bg, p-8, hover:shadow-lg transition. Custom SVG icon (minimal, 48×48, stroke-based per MASTER.md section 8), service name in font-display text-2xl, 2-sentence description in DM Sans 16px text-secondary. Services: Hardscape Design, Softscape & Planting, Pool & Surround, Outdoor Kitchens, Full Property Design. Link each to /services/[slug].
    - FeaturedProject: bg-forest-deep py-section. Two-column lg: left = large Unsplash image (The Adirondack Terrace — stone terrace, lake view), right = project details. Right col: eyebrow "FEATURED PROJECT" in gold, headline in font-display text-white, location + category in DM Sans text-white/60, description 2 sentences, investment range in text-gold, "View Case Study" Button variant="ghost".
  </behavior>
  <action>
    Each component must be a "use client" file when it uses Framer Motion directly, OR use the MotionDiv/MotionSection wrappers from src/components/ui/MotionDiv.tsx (which are already "use client"). The simplest pattern: make each section component "use client" since they all animate.

    For all motion components: wrap scroll-triggered sections in `whileInView="visible"` `initial="hidden"` `viewport={{ once: true, margin: "-80px" }}` using variants from src/constants/animation.ts. HeroSection triggers on mount (no viewport trigger needed).

    For reduced motion: add `const prefersReducedMotion = useReducedMotion()` in each animated component. If true, pass `initial={false}` and `animate={false}` (skip animations entirely — do not just reduce duration).

    For Unsplash images: choose real, high-quality landscape photography IDs. All images must use Next.js `Image` component with proper `alt` text. For fill images (hero, featured): parent must have `position: relative` and explicit height. For fixed-dimension images: provide width and height props.

    Service card SVG icons: inline SVG, 48×48 viewBox, strokeWidth 1.5, currentColor, no fill. Design each as an abstract minimal shape representing the service (stone/grid for hardscape, leaf-curve for softscape, water-ripple concentric for pool, flame for kitchen, property-outline for full property).

    All spacing: use Tailwind `py-section` (128px) for desktop section padding, `py-section-sm` (80px) on mobile via responsive prefix. Content containers: `max-w-7xl mx-auto px-6 md:px-12 lg:px-20`.
  </action>
  <verify>
    <automated>cd C:/Users/georg/my-project/verdant && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    All 5 components exist and TypeScript build succeeds. Each component renders its expected visual structure. No `any` types. All motion components check useReducedMotion. No broken image paths.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build sections 6–10 (ProcessTeaser, SeasonalBanner, Testimonials, InstagramPlaceholder, FinalCTA) and assemble home page</name>
  <files>
    src/components/home/ProcessTeaser.tsx,
    src/components/home/SeasonalBanner.tsx,
    src/components/home/Testimonials.tsx,
    src/components/home/InstagramPlaceholder.tsx,
    src/components/home/FinalCTA.tsx,
    src/app/page.tsx
  </files>
  <behavior>
    - ProcessTeaser: bg-forest-deep py-section. SectionHeader eyebrow="HOW IT WORKS" headline="From Vision to Reality" (text-white, eyebrow in gold per dark section rule). 3 steps displayed horizontally on lg, vertically stacked on mobile. Each step: large number (Cormorant Garamond 96px text-gold/20), step name (font-display text-white text-2xl), 2-sentence description (DM Sans text-white/60). Steps: 1. Discovery Consultation, 2. Design & Proposal, 3. Expert Installation. Connecting line between steps on lg (1px gold/30 horizontal rule). fadeUp with stagger on step cards.
    - SeasonalBanner: narrow strip (py-5), bg-gold. Centered text: "Now Booking Spring & Summer 2026 — Limited Slots Available" in DM Sans 500 text-forest-deep uppercase tracking-[0.1em] text-sm. No animation needed on this strip.
    - Testimonials: bg-forest-mid py-section. SectionHeader eyebrow="CLIENT STORIES" headline="What Our Clients Say" (dark=true). 2-column grid lg / 1-col sm. Per MASTER.md Testimonial Card anatomy: forest-mid bg, project photo top (200px, object-cover), then padding-32px body with: gold quote mark (Cormorant Garamond 72px text-gold/60 leading-none), quote in Cormorant Garamond italic 22px text-white, 32px gold divider rule, client name DM Sans 500 14px white, location+project DM Sans 300 13px text-white/60. Testimonial 1: "The terrace Verdant built has completely transformed how we use our property..." — Margaret & Thomas K., Lake George | Adirondack Terrace. Testimonial 2: "From our first consultation to the final reveal, the Verdant team demonstrated..." — Sarah L., Saratoga Springs | Willowmere Garden.
    - InstagramPlaceholder: bg-stone-warm py-section. SectionHeader eyebrow="FOLLOW OUR WORK" headline="@VerdantLandscape". 6-cell grid (3×2), each cell: square aspect-ratio, Unsplash landscape images, object-cover, hover:opacity-90 transition. Below grid: "Follow on Instagram" ghost-dark Button.
    - FinalCTA: bg-forest-deep py-section. Full-width dark section. Centered content: eyebrow "START YOUR PROJECT" in gold, headline in font-display text-white text-4xl md:text-5xl "Your Outdoor World Awaits", 2-sentence subline DM Sans text-white/70, "Start a Project" primary Button + "View Our Work" ghost Button side-by-side. fadeUp animation on entrance.
    - src/app/page.tsx: Server Component. Import all 10 section components and compose in order: HeroSection, LogoBar, ManifestoSection, ServicesPreview, FeaturedProject, ProcessTeaser, SeasonalBanner, Testimonials, InstagramPlaceholder, FinalCTA. Wrap with `<main>`. No props needed — all data is internal to each component.
  </behavior>
  <action>
    ProcessTeaser: Add the connecting horizontal line between steps using absolute positioning within the steps container on large screens only. Use a gold/30 opacity 1px div absolute positioned vertically centered. Steps must be relative-positioned.

    SeasonalBanner: This section does NOT use py-section spacing — it's a narrow strip. Use py-5 only. Background is bg-gold (from Tailwind token, resolves to #C9A84C). Text must be forest-deep for contrast.

    Testimonials: Project photos are Unsplash images at 400×200, object-cover. Hardcode two distinct landscape images (terrace/stone for first, garden/path for second). The large quote mark is literal `"` character in Cormorant Garamond — do not use a Lucide icon.

    InstagramPlaceholder: 6 different Unsplash landscape images at 400×400 square crop. All must have descriptive alt text. The "Follow on Instagram" button is non-functional in this phase (href="#") — it will be wired in Phase 6.

    FinalCTA: Both buttons render side-by-side with gap-4 on sm+, stacked on mobile. "View Our Work" button href="/portfolio".

    src/app/page.tsx: Keep as a Server Component (no "use client"). Just assemble the 10 section components in order. Add export const metadata for the home page:
    ```typescript
    export const metadata = {
      title: 'Verdant Landscape Design | We Build Outdoor Worlds',
      description: 'Luxury landscape design for the Adirondack region. Hardscape, pools, outdoor kitchens, and full property transformations.',
    }
    ```

    Verify the full section order in the final page.tsx matches ROADMAP success criteria: Hero → Logo Bar → Manifesto → Services Preview → Featured Project → Process Teaser → Seasonal Banner → Testimonials → Instagram Placeholder → Final CTA.
  </action>
  <verify>
    <automated>cd C:/Users/georg/my-project/verdant && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npm run build` exits 0. src/app/page.tsx imports and assembles all 10 section components. Each component file exists. No TypeScript errors. Home page section order matches ROADMAP spec.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External CDN → Browser | Unsplash images loaded via Next.js Image (optimized, proxied through /_next/image) |
| User input → Page | No user input on home page — fully static render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information | Unsplash image URLs | accept | Images are public-domain; no credentials embedded in URLs |
| T-03-02-02 | Denial of Service | next/image optimization | accept | Next.js Image handles sizing/format automatically; no custom resize logic to attack |
| T-03-02-03 | Elevation of Privilege | Instagram href="#" placeholder | accept | Non-functional placeholder; no real OAuth or token in this phase |
</threat_model>

<verification>
After both tasks complete:

1. `npm run dev` — visit localhost:3000
2. All 10 sections visible by scrolling top to bottom
3. Hero loads with visible Unsplash image (not broken) and gradient overlay
4. Hero headline text is Cormorant Garamond (visually serif and editorial)
5. Scroll down — LogoBar strip appears, then manifesto, services, featured project
6. Services section shows all 5 cards with SVG icons
7. ProcessTeaser has dark forest-deep background with 3 steps
8. SeasonalBanner is a gold strip with forest-deep text
9. Testimonials section shows 2 cards with quotes
10. Instagram Placeholder shows 6 image grid
11. FinalCTA is dark section at bottom before footer
12. Open DevTools console — zero errors
13. Enable "prefers-reduced-motion" in DevTools (Rendering → Emulate CSS media) — page renders without animations, no layout shift
14. `npm run build` exits 0
</verification>

<success_criteria>
- All 10 section components exist in src/components/home/
- src/app/page.tsx assembles them in the specified order
- `npm run build` exits 0 with no TypeScript errors
- Zero console errors in browser
- All Unsplash images load (no broken image icons)
- Cormorant Garamond renders on hero headline (not system serif)
- SeasonalBanner renders with gold background and forest-deep text
- Testimonials section renders 2 complete testimonial cards
- All motion components pass useReducedMotion check
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-and-portfolio-pages/03-02-SUMMARY.md` documenting:
- All 10 section components created and their file paths
- Unsplash image IDs used (so they can be swapped for client images later)
- Any design deviations from MASTER.md and why
- Any TypeScript or build issues encountered and resolved
</output>
